// Array of each game rules

const brainEven = 'Answer "yes" if the number is even, otherwise answer "no".';
const brainCalc = 'What is the result of the expression?';
const brainGcd = 'Find the greatest common divisor of given numbers.';

const gamesRules = [brainEven, brainCalc, brainGcd];

export default gamesRules;
